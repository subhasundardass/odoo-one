<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- ================= Account Tree View ================= -->
    <record id="view_account_account_tree" model="ir.ui.view">
        <field name="name">account.account.tree</field>
        <field name="model">account.account</field>
        <field name="arch" type="xml">
            <tree string="Accounts">
                <field name="code" />
                <field name="name" />
            </tree>
        </field>
    </record>

    <!-- ================= Account Form View ================= -->
    <record id="view_account_account_form" model="ir.ui.view">
        <field name="name">account.account.form</field>
        <field name="model">account.account</field>
        <field name="arch" type="xml">
            <form string="Account">
                <sheet>
                    <group>
                        <field name="code" />
                        <field name="name" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ================= Actions ================= -->
    <record id="action_ds_accounts_account" model="ir.actions.act_window">
        <field name="name">Chart of Accounts</field>
        <field name="res_model">account.account</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click 'Create' to add a new account.
            </p>
        </field>
    </record>


</odoo>